import SwiftUI

enum Typography {

    static let heading = Font.system(size: 11, weight: .semibold)

    static let headingTracking: CGFloat = 0.3

    static let caption = Font.system(size: 11)

    static let row = Font.system(size: 12)
}
